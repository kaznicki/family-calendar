---
phase: 01-grid-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/index.css
  - src/main.tsx
  - partykit.json
  - src/test/setup.ts
  - src/lib/dates.test.ts
  - src/lib/token.test.ts
  - src/components/WeekRow.test.tsx
  - src/components/DayColumn.test.tsx
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - VISU-01
  - VISU-04
  - SHRG-01

must_haves:
  truths:
    - "Running `npm run dev` starts a Vite dev server without errors"
    - "Running `npx vitest run` exits without crashing (tests may be pending/todo, not failing)"
    - "Tailwind v4 styles are processed: `@import \"tailwindcss\"` in src/index.css produces output"
    - "Test stubs exist for all Wave 0 files (dates.test.ts, token.test.ts, WeekRow.test.tsx, DayColumn.test.tsx)"
  artifacts:
    - path: "package.json"
      provides: "All npm dependencies declared"
      contains: "date-fns, yjs, y-partykit, tailwindcss, vitest"
    - path: "vite.config.ts"
      provides: "Vite + Tailwind v4 + Vitest config"
      exports: []
    - path: "src/test/setup.ts"
      provides: "Vitest test environment bootstrap"
      contains: "@testing-library/jest-dom/vitest"
    - path: "src/lib/dates.test.ts"
      provides: "Wave 0 stub: failing tests for generateWeeks + formatWeekRange"
    - path: "src/lib/token.test.ts"
      provides: "Wave 0 stub: failing test for getTokenFromURL"
    - path: "src/components/WeekRow.test.tsx"
      provides: "Wave 0 stub: failing test for WeekRow 7-column structure"
    - path: "src/components/DayColumn.test.tsx"
      provides: "Wave 0 stub: failing tests for today + weekend classes"
  key_links:
    - from: "vite.config.ts"
      to: "src/test/setup.ts"
      via: "test.setupFiles config"
      pattern: "setupFiles.*setup"
    - from: "src/index.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import \"tailwindcss\""
---

<objective>
Bootstrap the Vite + React + TypeScript project, configure Tailwind v4 and Vitest, and write Wave 0 test stubs so every subsequent plan has a verified test runner to work against.

Purpose: Nothing else can be built or tested without the project scaffold. Test stubs must exist before implementation plans so `<automated>` verify commands resolve to real test files from day one.
Output: A running dev server, a green (or pending) test suite, and four test files with todo/skip stubs for every Phase 1 behavior.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-grid-scaffold/01-CONTEXT.md
@.planning/phases/01-grid-scaffold/01-RESEARCH.md
@.planning/phases/01-grid-scaffold/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project and install all dependencies</name>
  <files>
    package.json,
    vite.config.ts,
    src/index.css,
    src/main.tsx,
    partykit.json
  </files>
  <action>
    Run the Vite scaffold command in the project root — but the directory already exists, so initialize via npm:

    ```bash
    cd "C:/Users/redjo/Documents/projects/family-calendar"
    # Scaffold React + TypeScript template into current directory
    npm create vite@latest . -- --template react-ts --force
    ```

    If the directory is already a git repo with files, scaffold into a temp dir and copy the generated files:
    ```bash
    mkdir /tmp/fc-scaffold && npm create vite@latest /tmp/fc-scaffold -- --template react-ts
    # Then copy: vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts
    ```

    After scaffold, install all dependencies in one pass:
    ```bash
    npm install date-fns yjs y-partykit@0.0.33
    npm install tailwindcss @tailwindcss/vite
    npm install -D partykit vitest jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom
    ```

    Replace `vite.config.ts` with the Tailwind v4 + Vitest config:
    ```typescript
    // vite.config.ts
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
      test: {
        globals: true,
        environment: 'jsdom',
        setupFiles: './src/test/setup.ts',
      },
    })
    ```

    Replace `src/index.css` contents with:
    ```css
    @import "tailwindcss";

    @theme {
      --color-today-bg: oklch(0.95 0.03 240);
      --color-weekend-bg: oklch(0.97 0 0);
    }

    * {
      box-sizing: border-box;
    }
    ```

    Replace `src/main.tsx` with a minimal entry (remove Vite boilerplate App content):
    ```tsx
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import './index.css'
    import App from './App.tsx'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>,
    )
    ```

    Replace `src/App.tsx` with a placeholder (real implementation in Plan 03):
    ```tsx
    export default function App() {
      return <div className="min-h-screen bg-white">Calendar loading…</div>
    }
    ```

    Create `partykit.json` at project root:
    ```json
    {
      "name": "family-calendar",
      "main": "party/server.ts"
    }
    ```

    Do NOT install or configure `tailwind.config.js` — Tailwind v4 uses CSS-first config via `@theme {}` in `index.css`. Do NOT use the PostCSS plugin approach — use `@tailwindcss/vite` plugin only.

    Pin y-partykit to exactly `0.0.33` in package.json (pre-stable; do not use `^` or `latest`).
  </action>
  <verify>
    <automated>cd "C:/Users/redjo/Documents/projects/family-calendar" && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run dev` starts without errors
    - `node_modules` contains date-fns, yjs, y-partykit, tailwindcss, vitest
    - `partykit.json` exists at project root
    - `src/index.css` contains `@import "tailwindcss"` and `@theme {}` block
    - `vite.config.ts` contains both `react()` and `tailwindcss()` plugins and a `test` block
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Wave 0 test stubs and Vitest setup</name>
  <files>
    src/test/setup.ts,
    src/lib/dates.test.ts,
    src/lib/token.test.ts,
    src/components/WeekRow.test.tsx,
    src/components/DayColumn.test.tsx
  </files>
  <action>
    Create the shared Vitest setup file:
    ```typescript
    // src/test/setup.ts
    import '@testing-library/jest-dom/vitest'
    import { afterEach } from 'vitest'
    import { cleanup } from '@testing-library/react'

    afterEach(() => {
      cleanup()
    })
    ```

    Create Wave 0 stub for date math (tests will FAIL — that is expected and correct for Wave 0):
    ```typescript
    // src/lib/dates.test.ts
    import { describe, it, expect } from 'vitest'
    // generateWeeks and formatWeekRange don't exist yet — Plan 02 implements them
    // These stubs define the expected contract before implementation

    describe('generateWeeks', () => {
      it.todo('returns ~104 weeks for a 24-month range (6 months back + 18 months forward)')
      it.todo('first week starts on a Sunday')
      it.todo('each week object has weekStart, weekEnd, days (length 7), and label')
      it.todo('today\'s date falls within the generated range')
    })

    describe('formatWeekRange', () => {
      it.todo('formats same-month range as "May 11–17"')
      it.todo('formats cross-month range as "May 28–Jun 3"')
    })
    ```

    Create Wave 0 stub for token reading:
    ```typescript
    // src/lib/token.test.ts
    import { describe, it, expect } from 'vitest'
    // getTokenFromURL doesn't exist yet — Plan 02 implements it

    describe('getTokenFromURL', () => {
      it.todo('reads ?token= query param from window.location.search')
      it.todo('returns empty string when token param is absent')
    })
    ```

    Create Wave 0 stub for WeekRow component:
    ```tsx
    // src/components/WeekRow.test.tsx
    import { describe, it } from 'vitest'
    // WeekRow component doesn't exist yet — Plan 04 implements it

    describe('WeekRow', () => {
      it.todo('renders exactly 7 DayColumn children')
      it.todo('renders the week-range label text')
    })
    ```

    Create Wave 0 stub for DayColumn component:
    ```tsx
    // src/components/DayColumn.test.tsx
    import { describe, it } from 'vitest'
    // DayColumn component doesn't exist yet — Plan 04 implements it

    describe('DayColumn — today', () => {
      it.todo('sets data-today="true" on today\'s date column')
      it.todo('does not set data-today on non-today columns')
    })

    describe('DayColumn — weekend', () => {
      it.todo('Saturday column has weekend background class')
      it.todo('Sunday column has weekend background class')
      it.todo('weekday column does not have weekend background class')
    })

    describe('DayColumn — slots', () => {
      it.todo('renders exactly 5 event slot children')
    })
    ```

    Use `it.todo()` (not `it.skip()`) so Vitest reports them as "todo" (passing the suite) rather than "skipped failures". The suite must exit 0 after this task — no actual test failures.
  </action>
  <verify>
    <automated>cd "C:/Users/redjo/Documents/projects/family-calendar" && npx vitest run --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `npx vitest run` exits with code 0 (todo tests do not count as failures)
    - Four test stub files exist: dates.test.ts, token.test.ts, WeekRow.test.tsx, DayColumn.test.tsx
    - `src/test/setup.ts` exists and imports `@testing-library/jest-dom/vitest`
    - No "Cannot find module" errors in the test output
  </done>
</task>

</tasks>

<verification>
Run the full suite: `cd "C:/Users/redjo/Documents/projects/family-calendar" && npx vitest run`

Expected: exits 0, all tests reported as "todo", zero failures, zero errors.

Also verify dev server starts: `npx vite build` completes without errors.
</verification>

<success_criteria>
- All npm dependencies installed and importable (date-fns, yjs, y-partykit@0.0.33, tailwindcss, vitest)
- Vite builds without errors
- `npx vitest run` exits 0 — test stubs registered as todo, not failing
- No tailwind.config.js created (v4 CSS-first config only)
- y-partykit pinned to exactly 0.0.33 in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-grid-scaffold/01-01-SUMMARY.md`
</output>
