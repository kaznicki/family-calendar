---
phase: 01-grid-scaffold
plan: 02
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/dates.ts
  - src/lib/dates.test.ts
  - src/lib/token.ts
  - src/lib/token.test.ts
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - SHRG-01

must_haves:
  truths:
    - "generateWeeks() returns ~104 week objects covering 6 months back to 18 months forward"
    - "Each week object has weekStart (Sunday), days array of 7 dates, and a formatted label"
    - "formatWeekRange() produces 'May 11–17' for same-month and 'May 28–Jun 3' for cross-month"
    - "getTokenFromURL() reads the ?token= query param and returns empty string when absent"
    - "All unit tests pass: npx vitest run src/lib/dates.test.ts and src/lib/token.test.ts"
  artifacts:
    - path: "src/lib/dates.ts"
      provides: "Week generation and date classification helpers"
      exports: ["generateWeeks", "formatWeekRange", "isDayWeekend", "isDayToday"]
    - path: "src/lib/token.ts"
      provides: "URL token extraction for PartyKit auth"
      exports: ["getTokenFromURL"]
    - path: "src/lib/dates.test.ts"
      provides: "Passing unit tests for all date math functions"
    - path: "src/lib/token.test.ts"
      provides: "Passing unit tests for getTokenFromURL"
  key_links:
    - from: "src/lib/dates.ts"
      to: "date-fns"
      via: "named imports"
      pattern: "from 'date-fns'"
    - from: "src/lib/token.ts"
      to: "window.location.search"
      via: "URLSearchParams"
      pattern: "URLSearchParams"
---

<objective>
Implement and test the date math library and URL token extractor using TDD. These are the two pure-function modules that everything else in Phase 1 depends on for data.

Purpose: Date generation drives the entire grid render. Token extraction wires the security layer. Both have well-defined inputs and outputs — ideal TDD candidates.
Output: src/lib/dates.ts and src/lib/token.ts with passing unit tests, replacing the Wave 0 todo stubs with real assertions.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-grid-scaffold/01-RESEARCH.md
@.planning/phases/01-grid-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- Key API from date-fns v4 used by dates.ts -->
```typescript
// All from 'date-fns'
eachWeekOfInterval(interval: { start: Date; end: Date }, options?: { weekStartsOn: 0 | 1 | ... }): Date[]
addDays(date: Date, amount: number): Date
addMonths(date: Date, amount: number): Date
subMonths(date: Date, amount: number): Date
format(date: Date, formatStr: string): string
isToday(date: Date): boolean
isSaturday(date: Date): boolean
isSunday(date: Date): boolean
```
</interfaces>
</context>

<feature>
  <name>Date math library (dates.ts) — TDD</name>
  <files>src/lib/dates.ts, src/lib/dates.test.ts</files>
  <behavior>
    Replace the todo stubs in dates.test.ts with real assertions, then implement dates.ts to make them pass.

    RED phase — replace todos with real tests:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest'
    import { generateWeeks, formatWeekRange, isDayWeekend, isDayToday } from './dates'

    describe('generateWeeks', () => {
      it('returns ~104 weeks for a 24-month range', () => {
        const today = new Date(2026, 2, 10) // March 10, 2026 — fixed for determinism
        const weeks = generateWeeks(today)
        // 6 months back + 18 months forward ≈ 24 months ≈ 104 weeks
        expect(weeks.length).toBeGreaterThanOrEqual(100)
        expect(weeks.length).toBeLessThanOrEqual(110)
      })

      it('first week starts on a Sunday (weekStartsOn: 0)', () => {
        const today = new Date(2026, 2, 10)
        const weeks = generateWeeks(today)
        expect(weeks[0].weekStart.getDay()).toBe(0) // 0 = Sunday
      })

      it('each week has 7 days', () => {
        const today = new Date(2026, 2, 10)
        const weeks = generateWeeks(today)
        weeks.slice(0, 10).forEach((w) => {
          expect(w.days).toHaveLength(7)
        })
      })

      it('today falls within the generated range', () => {
        const today = new Date(2026, 2, 10)
        const weeks = generateWeeks(today)
        const allDays = weeks.flatMap((w) => w.days)
        const todayStr = today.toDateString()
        expect(allDays.some((d) => d.toDateString() === todayStr)).toBe(true)
      })
    })

    describe('formatWeekRange', () => {
      it('formats same-month range as "May 11–17"', () => {
        const start = new Date(2026, 4, 11)  // May 11
        const end = new Date(2026, 4, 17)    // May 17
        expect(formatWeekRange(start, end)).toBe('May 11–17')
      })

      it('formats cross-month range as "May 28–Jun 3"', () => {
        const start = new Date(2026, 4, 28)  // May 28
        const end = new Date(2026, 5, 3)     // Jun 3
        expect(formatWeekRange(start, end)).toBe('May 28–Jun 3')
      })
    })

    describe('isDayWeekend', () => {
      it('returns true for Saturday', () => {
        expect(isDayWeekend(new Date(2026, 2, 14))).toBe(true) // Sat
      })
      it('returns true for Sunday', () => {
        expect(isDayWeekend(new Date(2026, 2, 15))).toBe(true) // Sun
      })
      it('returns false for Monday', () => {
        expect(isDayWeekend(new Date(2026, 2, 16))).toBe(false) // Mon
      })
    })
    ```

    GREEN phase — implement dates.ts to pass all tests:
    Use the exact pattern from RESEARCH.md (Pattern 5). Use `eachWeekOfInterval` with `weekStartsOn: 0`.
    The en-dash character in "May 11–17" must be `\u2013` (–), not a hyphen (-).

    Do NOT use `getDay()` arithmetic for week generation. Use `eachWeekOfInterval` from date-fns.
    Do NOT store or compute display positions. Export only: `generateWeeks`, `formatWeekRange`, `isDayWeekend`, `isDayToday`.
  </behavior>
  <implementation>
    After RED is confirmed failing and GREEN is confirmed passing:

    ```typescript
    // src/lib/dates.ts
    import {
      eachWeekOfInterval,
      addDays,
      addMonths,
      subMonths,
      format,
      isToday,
      isSaturday,
      isSunday,
    } from 'date-fns'

    export interface WeekData {
      weekStart: Date
      weekEnd: Date
      days: Date[]
      label: string
    }

    export function generateWeeks(today: Date = new Date()): WeekData[] {
      const start = subMonths(today, 6)
      const end = addMonths(today, 18)

      const weekStarts = eachWeekOfInterval({ start, end }, { weekStartsOn: 0 })

      return weekStarts.map((weekStart) => {
        const days = Array.from({ length: 7 }, (_, i) => addDays(weekStart, i))
        return {
          weekStart,
          weekEnd: days[6],
          days,
          label: formatWeekRange(weekStart, days[6]),
        }
      })
    }

    export function formatWeekRange(start: Date, end: Date): string {
      if (format(start, 'MMM') === format(end, 'MMM')) {
        return `${format(start, 'MMM d')}\u2013${format(end, 'd')}`
      }
      return `${format(start, 'MMM d')}\u2013${format(end, 'MMM d')}`
    }

    export function isDayWeekend(date: Date): boolean {
      return isSaturday(date) || isSunday(date)
    }

    export function isDayToday(date: Date): boolean {
      return isToday(date)
    }
    ```
  </implementation>
</feature>

<feature>
  <name>Token URL extractor (token.ts) — TDD</name>
  <files>src/lib/token.ts, src/lib/token.test.ts</files>
  <behavior>
    Replace the todo stubs in token.test.ts with real assertions, then implement token.ts.

    RED phase — replace todos with real tests:
    ```typescript
    import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
    import { getTokenFromURL } from './token'

    describe('getTokenFromURL', () => {
      const originalLocation = window.location

      beforeEach(() => {
        // jsdom allows reassigning window.location.search via Object.defineProperty
        Object.defineProperty(window, 'location', {
          writable: true,
          value: { ...originalLocation, search: '' },
        })
      })

      afterEach(() => {
        Object.defineProperty(window, 'location', {
          writable: true,
          value: originalLocation,
        })
      })

      it('reads ?token= query param from window.location.search', () => {
        window.location.search = '?token=abc123secret'
        expect(getTokenFromURL()).toBe('abc123secret')
      })

      it('returns empty string when token param is absent', () => {
        window.location.search = ''
        expect(getTokenFromURL()).toBe('')
      })

      it('returns empty string when search is only other params', () => {
        window.location.search = '?foo=bar'
        expect(getTokenFromURL()).toBe('')
      })
    })
    ```

    GREEN phase — implement token.ts:
    Use `new URLSearchParams(window.location.search)` — do NOT use `location.href.split('?')[1]`.
  </behavior>
  <implementation>
    ```typescript
    // src/lib/token.ts
    export function getTokenFromURL(): string {
      const params = new URLSearchParams(window.location.search)
      return params.get('token') ?? ''
    }
    ```
  </implementation>
</feature>

<verification>
```bash
cd "C:/Users/redjo/Documents/projects/family-calendar"
npx vitest run src/lib/dates.test.ts src/lib/token.test.ts --reporter=verbose
```

All tests must pass (not todo — real assertions). Zero failures, zero errors.
</verification>

<success_criteria>
- `npx vitest run src/lib/dates.test.ts` — all tests pass (not todo stubs)
- `npx vitest run src/lib/token.test.ts` — all tests pass
- generateWeeks returns 100–110 weeks for a 24-month range
- formatWeekRange uses en-dash (–) not hyphen (-)
- isDayWeekend correctly identifies Saturday and Sunday
- getTokenFromURL reads URLSearchParams (not string splitting)
- TypeScript compiles without errors on both files
</success_criteria>

<output>
After completion, create `.planning/phases/01-grid-scaffold/01-02-SUMMARY.md`
</output>
