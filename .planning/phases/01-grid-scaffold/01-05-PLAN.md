---
phase: 01-grid-scaffold
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - VISU-01
  - VISU-04
  - SHRG-01

must_haves:
  truths:
    - "All 7 day columns are visible simultaneously on a 375px viewport without horizontal scroll"
    - "Day-name header row stays fixed at top while calendar body scrolls"
    - "Weekend (Sat, Sun) columns have a visually distinct gray background compared to weekday columns"
    - "Today's column has a visually distinct highlight (different from weekend and weekday)"
    - "Current week is visible near the top on page load (not the beginning of the 6-month-back range)"
    - "Vertical scrolling reveals past and future weeks continuously"
  artifacts:
    - path: "src/components/CalendarGrid.tsx"
      provides: "Running, rendered calendar grid"
    - path: "party/server.ts"
      provides: "PartyKit server accepting token-validated connections"
  key_links:
    - from: "browser at localhost:5173"
      to: "CalendarGrid component"
      via: "Vite dev server HMR"
      pattern: "localhost:5173"
---

<objective>
Human visual verification of the running Phase 1 calendar. Confirms that the grid looks correct in a real browser, at mobile viewport width, before closing the phase.

Purpose: Automated tests verify logic (correct date math, correct classes), but they cannot verify that the CSS Grid layout actually renders without overflow at 375px, that the sticky header works in a real browser context, or that the visual style is appropriate for family use.
Output: Phase 1 signed off by user or issues reported for gap closure.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-grid-scaffold/01-CONTEXT.md
@.planning/phases/01-grid-scaffold/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dev server and confirm test suite green</name>
  <files></files>
  <action>
    Start the Vite dev server:
    ```bash
    cd "C:/Users/redjo/Documents/projects/family-calendar"
    npm run dev
    ```

    The dev server will start at http://localhost:5173 (default Vite port).

    Also confirm the full test suite is green one final time before handing off to the user:
    ```bash
    npx vitest run --reporter=dot
    ```

    Note: PartyKit dev server is NOT required for Phase 1 visual verification. The YPartyKitProvider will attempt to connect to localhost:1999 and fail silently — the grid renders from date math alone, with no data from the server. This is expected and correct behavior for Phase 1.
  </action>
  <verify>
    <automated>cd "C:/Users/redjo/Documents/projects/family-calendar" && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>
    - npx vitest run exits 0
    - npm run dev starts without build errors
    - App accessible at http://localhost:5173
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of calendar grid</name>
  <files></files>
  <action>
    Present the verification checklist to the user and wait for approval.
    The dev server is running at http://localhost:5173 from Task 1.
    No code changes are made in this task — this is a human review gate.
  </action>
  <verify>
    <automated>MISSING — verification is human-performed in the browser (see what-built and how-to-verify below)</automated>
  </verify>
  <done>User types "approved" after confirming all 7 visual checks pass in the browser.</done>
  <what-built>
    Complete Phase 1 calendar grid scaffold:
    - 7-column weekly grid with date-range headers
    - Sticky day-name header strip (M T W T F S S on mobile, full names on desktop)
    - 5 empty event slots per day column
    - Weekend (Sat/Sun) gray background
    - Today's column highlighted (blue tint)
    - Grid opens scrolled to current week
    - PartyKit token-protected server scaffold (not yet connected in dev — expected)
    - Full Vitest unit + component test suite passing
  </what-built>
  <how-to-verify>
    Open http://localhost:5173 in Chrome.

    **Check 1 — Mobile layout (most important):**
    Open Chrome DevTools (F12) → click the device toolbar icon → set width to 375px.
    Confirm: All 7 day columns are visible at once. No horizontal scrollbar. Columns are narrow but readable.

    **Check 2 — Sticky header:**
    Scroll down through weeks. Confirm: The day-name header row (S M T W T F S) stays fixed at the top while the week rows scroll underneath.

    **Check 3 — Today highlight:**
    Find today's date column. Confirm: It has a visually distinct background (should be a light blue tint) different from both weekday columns (white) and weekend columns (light gray).

    **Check 4 — Weekend shading:**
    Find any Saturday or Sunday column. Confirm: Both weekend columns have a subtle gray background — slightly different from white weekday columns, but not dominant or distracting.

    **Check 5 — Scroll position:**
    On page load (or hard refresh), confirm: The current week is visible near the top of the viewport — NOT the first week from 6 months ago.

    **Check 6 — Vertical scroll:**
    Scroll down and up through multiple weeks. Confirm: Weeks flow continuously with date-range labels (e.g., "Mar 8–14"). No layout breaks or horizontal overflow at any scroll position.

    **Check 7 — Desktop layout:**
    Expand DevTools to a normal desktop width (1200px+). Confirm: Day names expand to full (Sun Mon Tue...), columns are comfortably wide, no visual regressions.

    **Acceptable issues (fix in Phase 2 or 3):**
    - Console WebSocket error connecting to localhost:1999 — expected (PartyKit not running)
    - Empty slot rows are completely invisible — correct (Phase 1 design decision)
    - No events visible — correct (event creation is Phase 2)

    **Report blockers if:**
    - Any column overflows horizontally at 375px
    - Sticky header scrolls away with content
    - Today column is not distinguishable from other columns
    - Weekend columns are not distinguishable from weekday columns
    - Grid loads showing dates from months ago (scroll-to-today broken)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass. Or describe any issues found (e.g., "sticky header broken", "mobile overflow on DayColumn") and I will fix them before closing the phase.
  </resume-signal>
</task>

</tasks>

<verification>
All verification is performed by the user in the browser per the checkpoint instructions above.

Final automated gate before user review:
```bash
cd "C:/Users/redjo/Documents/projects/family-calendar" && npx vitest run
```
Must exit 0.
</verification>

<success_criteria>
User confirms "approved" after verifying:
1. 375px mobile: all 7 columns fit, no horizontal scroll
2. Sticky header stays fixed during scroll
3. Today column distinctly highlighted
4. Weekend columns have subtle gray background
5. Grid opens at current week (not 6 months ago)
6. Vertical scroll works continuously through all weeks
</success_criteria>

<output>
After user approves, create `.planning/phases/01-grid-scaffold/01-05-SUMMARY.md`
</output>
