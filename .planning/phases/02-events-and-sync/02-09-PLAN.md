---
phase: 02-events-and-sync
plan: 09
type: execute
wave: 6
depends_on:
  - 02-06
  - 02-07
  - 02-08
files_modified: []
autonomous: false
requirements:
  - EVNT-01
  - EVNT-02
  - EVNT-03
  - EVNT-04
  - PEPL-01
  - PEPL-02
  - SHRG-02
  - RECU-01

must_haves:
  truths:
    - "User can add a single-day event by clicking an empty slot — popover appears, Enter saves a colored chip"
    - "User can edit an existing event — clicking EventCard opens popover pre-filled with current data"
    - "User can delete an event — delete button in popover removes the colored chip from the grid"
    - "Multi-day event (drag Mon-Thu) appears as a single spanning block in slot row 0 across those columns"
    - "Person color chips in the popover visually match the Google Sheets screenshot colors"
    - "RecurringFooter is always visible at the bottom of the viewport while the calendar scrolls"
    - "Opening the same URL in two browser tabs — an event added in tab 1 appears in tab 2 within ~1 second"
  artifacts:
    - path: "src/components/EventCard.tsx"
      provides: "Verified colored chip rendering correct person colors"
    - path: "src/components/RecurringFooter.tsx"
      provides: "Verified fixed footer visible during grid scroll"
  key_links: []
---

<objective>
Human verification checkpoint: confirm all Phase 2 interactive behaviors work correctly in the browser, person colors match the Google Sheets screenshot, real-time sync works between two tabs, and the recurring footer stays anchored during scroll.

Purpose: Automated tests verify structure and behavior contracts. This checkpoint verifies visual quality (color accuracy), multi-device sync (requires live PartyKit), and gestural UX (drag-to-select) that cannot be automated with jsdom.
Output: Human confirmation that Phase 2 is visually and functionally complete.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-events-and-sync/02-CONTEXT.md
@Family-Calendar-Google-Sheets-03-10-2026_10_53_AM.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm dev servers start</name>
  <files>none</files>
  <action>
Run the full automated test suite and TypeScript check before human verification:

1. Run `npx vitest run` — must exit 0 with all tests passing
2. Run `npx tsc --noEmit` — must exit 0 with no TypeScript errors
3. Start the Vite dev server: `npm run dev` — note the localhost URL (typically http://localhost:5173)
4. In a separate terminal, start the PartyKit dev server: `npx partykit dev` — needed for real-time sync testing in Step 6

Report the local dev URL and confirm both servers started without errors. If any test fails or TypeScript has errors, fix before proceeding to the human verification step.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Full test suite exits 0. TypeScript has no errors. Dev server and PartyKit server started successfully. URL reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 2 features</name>
  <action>Claude has run the full test suite (exits 0) and started the dev server. Open the app URL and verify all Phase 2 features as described below.</action>
  <what-built>
Complete Phase 2 feature set:
- Single-day event CRUD (click to create, click to edit, delete button)
- Person color chips (7 people + unassigned) in event creation/edit popover
- Multi-day event creation via drag across day slots
- Multi-day events render as spanning blocks in slot row 0
- RecurringFooter: fixed panel at viewport bottom, 7 people x 7 days grid
- SettingsPanel: roster management via settings icon in top-right corner
- Real-time sync via Yjs + PartyKit
  </what-built>
  <how-to-verify>
Open http://localhost:5173/?token=your-token in your browser (use the token from your .env file).

**Step 1 — Single-day event create:**
1. Click any empty slot in a day column
2. Verify a white popover appears anchored near that slot
3. Type "Doctor appointment" in the title input
4. Click the "Lois" color chip (should be light pink)
5. Press Enter
6. Verify a pink colored chip appears in that slot with "Doctor appointment"

**Step 2 — Single-day event edit and delete:**
1. Click the "Doctor appointment" chip you just created
2. Verify the popover opens with the title pre-filled and the Lois chip highlighted
3. Change the title to "Dentist" and press Enter — verify the chip updates
4. Click the chip again, then click the Delete button — verify it disappears

**Step 3 — Color accuracy check:**
1. Open the Google Sheets screenshot: `Family-Calendar-Google-Sheets-03-10-2026_10_53_AM.png`
2. Create one event for each of the 7 people
3. Compare color chips against the Google Sheets column header colors:
   - Timur: salmon/coral, Lois: light pink, Joy: mint green, Ivy: periwinkle
   - Both girls: bright cyan, Whole Family: bright yellow, Other: light peach/tan
4. Note any colors significantly off (can be tuned in index.css @theme block)

**Step 4 — Multi-day event:**
1. Click and hold on a Monday slot, drag to Thursday (same week), release
2. Verify a popover appears — type "Beach vacation", select "Whole Family" (yellow), press Enter
3. Verify a yellow spanning block appears across Mon-Thu in slot row 0

**Step 5 — RecurringFooter:**
1. Scroll the calendar grid — verify the recurring footer stays pinned at the bottom
2. Click an empty cell in the footer (e.g., Timur's Monday cell)
3. Type "Capital Planning" and press Enter — verify it appears in that cell

**Step 6 — Real-time sync (requires two browser tabs):**
1. Open the app URL in a second browser tab (same URL with token)
2. In tab 1, add an event "Sync test" on any day
3. Switch to tab 2 — verify "Sync test" appears within ~1 second without refreshing
4. Delete the event in tab 2 — verify it disappears in tab 1

**Step 7 — Settings panel:**
1. Click the settings icon in the top-right corner
2. Verify the panel opens showing the 7 default family members
3. Add "Grandma" with any color, click Add
4. Verify "Grandma" appears in the list and in future event popovers

Report any issues. Approve if all steps pass (minor color approximations are acceptable).
  </how-to-verify>
  <verify>
    <automated>MISSING — visual and sync behaviors require human verification</automated>
  </verify>
  <done>Human confirms all 7 verification steps pass. Person colors approximately match Google Sheets. Real-time sync confirmed in two tabs.</done>
  <resume-signal>Type "approved" to complete Phase 2, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
- Full test suite exits 0 (automated — Task 1)
- All 7 verification steps pass (human — Task 2)
- Real-time sync confirmed in two tabs
- Person colors approximately match Google Sheets screenshot
- RecurringFooter visible and editable
- SettingsPanel opens and roster is configurable
</verification>

<success_criteria>
Human confirms: event CRUD works, person colors visible and approximately correct, multi-day spanning works, recurring footer fixed at bottom, real-time sync functional between two tabs, settings panel opens and roster is editable. All Phase 2 requirements verified in browser.
</success_criteria>

<output>
After completion, create `.planning/phases/02-events-and-sync/02-09-SUMMARY.md`
</output>
