---
phase: 03-visual-polish-and-print
plan: 05
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
files_modified: []
autonomous: false
requirements:
  - VISU-02
  - VISU-03
  - PEPL-03
  - PRNT-01

must_haves:
  truths:
    - "Holiday gray shading works via right-click and long-press on day column headers"
    - "Birthday amber tint and label appear correctly for birthday days"
    - "Print produces clean 7-column landscape grid for next 10 weeks in Chrome, Safari, and Firefox"
    - "All Phase 3 requirements verified by human"
  artifacts:
    - path: ".planning/phases/03-visual-polish-and-print/03-05-SUMMARY.md"
      provides: "Human verification sign-off"
  key_links: []
---

<objective>
Human verification checkpoint for Phase 3. Confirms all visual features work correctly end-to-end: holiday toggling, birthday display, birthday CRUD in settings, and the print view across three browsers.

Purpose: Phase 3 is purely visual — automated tests verify logic but cannot verify visual rendering, print layout, or mobile interactions. This checkpoint is the final gate before Phase 3 is marked complete.
Output: Human approval or list of issues for gap closure.
</objective>

<execution_context>
@C:/Users/redjo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redjo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-visual-polish-and-print/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and dev server preflight</name>
  <files></files>
  <action>
    Before human verification, confirm the codebase is in a clean state:
    1. Run `npx vitest run --reporter=verbose` — must be all green.
    2. Run `npx tsc --noEmit` — must be zero errors.
    3. Start the dev server: `npm run dev`.
    4. Open the app in a browser and confirm it loads without console errors.
    If any of steps 1-3 fail, stop and fix before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd /c/Users/redjo/Documents/projects/family-calendar && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass, TypeScript clean, dev server starts without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 3 visual features</name>
  <action>Claude has run the full test suite (exits 0) and confirmed TypeScript compiles clean. Open the app URL and verify all Phase 3 features as described below.</action>
  <what-built>
    Phase 3 Visual Polish and Print features:
    1. Holiday/vacation shading — manual per-day toggle via right-click (desktop) or long-press (mobile)
    2. Birthday/anniversary amber tint + label — user-editable in Settings panel
    3. Print view — next 10 weeks via Print button, browser @media print
  </what-built>
  <how-to-verify>
    Prerequisites: dev server running at http://localhost:5173 (or whichever port Vite uses).

    === Holiday Shading (VISU-02, VISU-03) ===

    1. On desktop: right-click any day column header (the date number, e.g. "17"). A small context menu should appear with "Mark as holiday". Click it.
       Expected: That day's column turns gray (same shade as Saturday/Sunday columns — oklch(0.93 0 0) / --color-weekend-bg).

    2. Right-click the same day again. The menu should say "Unmark holiday". Click it.
       Expected: The gray shading is removed; the column returns to its normal background.

    3. Mark 3-4 consecutive days as holidays (simulating a vacation week). Each should show gray independently.

    4. (Optional) On a mobile device or Chrome DevTools mobile emulation: long-press a day header for ~500ms.
       Expected: Same context menu appears. Native iOS callout (copy/look up/share) should NOT appear.

    === Birthday Display (PEPL-03) ===

    5. Open Settings (gear icon, top-right). Scroll to "Birthdays & Anniversaries" section at the bottom.
       Expected: Section exists below "Family Members" section.

    6. Add a birthday: enter "Mom" in the name field, month 3, day 17. Click "Add Birthday".
       Expected: "Mom — 3/17" appears in the birthdays list.

    7. Navigate the calendar to March 17 of any year (scroll if needed).
       Expected: March 17 column shows a warm gold/amber background (distinct from the gray weekend/holiday color) and a small label "Mom's bday" above the event slots.

    8. Verify that adding an event on March 17 still works — 4 slots available, event appears correctly.
       Expected: Birthday label is above the slots; all 4 event slots are still usable.

    9. Test conflict: mark March 17 as a holiday AND it has the birthday.
       Expected: The column shows GOLD (birthday amber), not gray (birthday takes priority).

    10. Remove the birthday entry from Settings (× button). Navigate back to March 17.
        Expected: Gold tint is gone, birthday label is gone.

    === Print View (PRNT-01) ===

    11. Click the "Print" button (visible top-right of the app, to the left of the gear icon).
        Expected: Browser print dialog opens.

    12. In the print preview (before confirming print):
        - The calendar shows exactly 10 weeks starting from the current week
        - Layout is landscape orientation (wider than tall)
        - 7 columns are visible without any column collapsing or wrapping
        - Week-range labels appear above each week row
        - Event chips are visible in their colored backgrounds
        - The settings gear icon, print button, and recurring footer are NOT visible in the print preview
        - Weekend columns show gray, holiday-marked columns show gray, birthday columns show amber

    13. Check in at least 2 browsers (Chrome + Firefox or Chrome + Safari):
        - Column widths look consistent
        - No week row splits across a page break mid-row (the break-inside: avoid should prevent this, though it may not always be honored)

    === Overall ===

    14. Open the app on a mobile screen (or DevTools responsive mode at 375px width).
        - Print button and gear are still accessible
        - Holiday context menu works via long-press
        - Birthday labels are visible (even if truncated)

  </how-to-verify>
  <resume-signal>
    Type "approved" if all verifications pass.
    Or describe specific issues (e.g., "step 4 failed — iOS callout appears", "step 12 Firefox — columns collapse") so gap closure can be planned.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval received. All Phase 3 requirements (VISU-02, VISU-03, PEPL-03, PRNT-01) confirmed working by user.
</verification>

<success_criteria>
- Human confirms holiday toggle works via right-click (desktop)
- Human confirms birthday amber tint and label appear correctly
- Human confirms birthday CRUD works in SettingsPanel
- Human confirms print preview shows 10-week landscape grid without column collapse
- Human types "approved" or issues are logged for gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-polish-and-print/03-05-SUMMARY.md`
</output>
